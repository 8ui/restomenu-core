# Only set auth token for GitLab registry, don't override default registry
# This allows npm to resolve @restomenu/core from the local workspace
# while still being able to publish to the GitLab registry when needed
//gitlab.com/api/v4/projects/62384896/packages/npm/:_authToken=${CI_JOB_TOKEN}

# For local development and CI/CD, we want to use workspace packages
# The registry configuration above can cause conflicts with local workspace resolution